name: Deploy

on:
  push:
    branches:
      - main

env:
  GCP_REGION: europe-west1
  BACKEND_SERVICE: virtual-tryon-server
  FRONTEND_SERVICE: virtual-tryon-client

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Deploy backend to Cloud Run
        working-directory: server
        env:
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          CICD_SA: ${{ secrets.GCP_WIF_SERVICE_ACCOUNT }}
          RUNTIME_SA: virtual-tryon-sa@${{ secrets.GCP_PROJECT_ID }}.iam.gserviceaccount.com
        run: |
          gcloud run deploy "${BACKEND_SERVICE}" \
            --source . \
            --region "${GCP_REGION}" \
            --platform managed \
            --allow-unauthenticated \
            --quiet \
            --service-account "${RUNTIME_SA}" \
            --build-service-account "projects/${PROJECT_ID}/serviceAccounts/${CICD_SA}" \
            --set-env-vars="GCP_PROJECT_ID=${PROJECT_ID},GOOGLE_CLOUD_LOCATION=${GCP_REGION},MODEL_NAME=virtual-try-on-001"

  deploy-frontend:
    runs-on: ubuntu-latest
    needs: deploy-backend
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Resolve backend URL
        id: backend
        run: |
          BACKEND_URL="$(gcloud run services describe "${BACKEND_SERVICE}" \
            --region "${GCP_REGION}" \
            --format='value(status.url)')"
          echo "url=${BACKEND_URL}" >> "${GITHUB_OUTPUT}"

      - name: Deploy frontend to Cloud Run
        working-directory: client
        env:
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          CICD_SA: ${{ secrets.GCP_WIF_SERVICE_ACCOUNT }}
          RUNTIME_SA: virtual-tryon-sa@${{ secrets.GCP_PROJECT_ID }}.iam.gserviceaccount.com
        run: |
          gcloud run deploy "${FRONTEND_SERVICE}" \
            --source . \
            --region "${GCP_REGION}" \
            --platform managed \
            --allow-unauthenticated \
            --quiet \
            --service-account "${RUNTIME_SA}" \
            --build-service-account "projects/${PROJECT_ID}/serviceAccounts/${CICD_SA}" \
            --set-build-env-vars="VITE_API_URL=${{ steps.backend.outputs.url }}"

      - name: Update backend CORS origin
        run: |
          FRONTEND_URL="$(gcloud run services describe "${FRONTEND_SERVICE}" \
            --region "${GCP_REGION}" \
            --format='value(status.url)')"
          gcloud run services update "${BACKEND_SERVICE}" \
            --region "${GCP_REGION}" \
            --update-env-vars="ALLOWED_ORIGIN=${FRONTEND_URL}"
